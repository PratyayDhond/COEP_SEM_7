#ifndef FUNCTIONS_H
#define FUNCTIONS_H

int getLength(const char* token);
const char* determine_type(const char* str);
const char* getValue(const char* str);
// bool is_keyword(const char* str);
// bool is_operator(const char* str);
#endif
    